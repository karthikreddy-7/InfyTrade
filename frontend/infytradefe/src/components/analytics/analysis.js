import React from "react";

function analysis() {
  // multiple drop down
  // 1. selectn only one chart
  // 2. select the type of chart.
  // 3. if chartSelected == line chart || etc give option to add one more stock into analysis
  // 4. after displaying the chart give options for hourly, weekly, monthly, yearly.
  return (
    <>
      <div>analysis</div>
    </>
  );
}

export default analysis;
