import React, { useEffect, useState } from "react";
import { useSelector } from "react-redux";

const StockChartGenerator = ({ ChartType, stock }) => {
  //console.log("StockChartGenerator", ChartType, stock);
  return (
    <>
      <div></div>
    </>
  );
};

export default StockChartGenerator;
